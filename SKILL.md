---
name: holdle-data
description: "HOLDLE 行情数据获取：按用户问题类型拉取 A股/港股/美股 月K/周K/日K/财报/实时行情（自动选择复权口径），配合 holdle-ai MCP 做投研分析。"
---

# HOLDLE 数据获取 Skill 📊

> 用户需要分析某只股票（当前状态 / 历史复盘 / 数据查询）时使用本 skill。
> 本 skill 只负责**拉取数据**，投资判断请配合 **holdle-ai MCP**（知识库检索）完成。

---

## 一、何时使用

用户提到以下意图时，自动触发本 skill：

- 「分析 XX 股票」「XX 怎么样」「帮我看看 XX」
- 「XX 现在是不是状态A」「是否准备突破」「当前入场参考价」
- 「帮我复盘 XX」「回测 XX 十年入场点」「分析 XX 年-XX 年的状态A」
- 任何需要股票行情数据（月K/周K/日K/财报/实时价格）的场景

**判断股票代码：** 从用户问题中识别（A股6位数字 / 美股英文代码 / 港股 hk_ 前缀）。识别不到时询问用户。

---

## 二、⚠️ 复权口径选择（2026-08-19 老板定稿，必须遵守）

**根据用户问题的时间维度选择复权方式：**

| 用户问题类型 | 复权 | 参数 | 理由 |
|:--|:--|:--|:--|
| **当前时点判断**：「XX现在是否状态A」「是否准备突破」「当前入场参考价怎么判断」「当前价格」 | **前复权** | `--adjust forward` | 前复权以当前价为基准，与用户行情软件默认显示一致，当前价格/参考价对照无偏差 |
| **近期时点判断（🆕 2026-08-22 收紧）**：「近期会不会有机会」「近期走势」「近一两年」「现在能不能关注」 | **前复权** | `--adjust forward` | 「近期」属于当前时点判断，必须与行情软件当前显示一致 |
| **历史复盘/回测**：「复盘XX」「回测XX十年」「分析2013-2016年状态A」「历史入场点」 | **后复权** | `--adjust backward`（默认） | 后复权以首日为基准，历史序列固定不变，回测复盘口径稳定 |
| **不确定/混合** | **一律前复权**（拿不准时按前复权） | `--adjust forward` | 宁可前复权（与用户看到的行情一致），不要默认后复权；只有明确「历史/复盘/回测」才用后复权 |

> 💡 判断口诀：**看「现在/近期」用前复权，看「过去/历史」用后复权。拿不准 → 前复权。**

---

## 三、执行步骤

### Step 1：运行数据脚本

```bash
python3 holdle_data.py <股票代码> <输出目录> [--adjust forward|backward]
```

示例：
```bash
# 当前状态分析（前复权）
python3 holdle_data.py 600519 /tmp/holdle_data --adjust forward

# 历史复盘（后复权，默认）
python3 holdle_data.py NVDA /tmp/holdle_data

# 指定输出目录
python3 holdle_data.py hk_00700 ~/holdle_data --adjust backward
```

脚本会生成：
- `{代码}_月K_{复权}.csv` — 月K线 + DIF/DEA/MACD柱
- `{代码}_周K_{复权}.csv` — 周K线 + MACD
- `{代码}_日K_{复权}.csv` — 日K线 + MACD
- `{代码}_财务_近20年.csv` — ROE/毛利率/净利率/现金流/负债率（A股/港股/美股）
- 实时行情打印在终端

### Step 2：读取数据 + 调 MCP 检索方法论

```python
# 读取 CSV（月K用于状态A判断，周K用于出场管理，日K用于入场点验证）
import pandas as pd
monthly = pd.read_csv(f"{OUT}/{code}_月K_{adjust_label}.csv")
```

同时调用 **holdle_ask** 工具检索方法论片段（如「状态A判断」「入场流程」「交易管理」）。

### Step 3：结合分析并输出

按 HOLDLE 方法论组织回答，**必须注明复权口径**：

> 「以下分析基于 {前复权/后复权} 数据。月K MACD 双线 {≥/＜}0轴、柱子 {红/绿}……按 HOLDLE 方法论……」

---

## 四、输出规范

1. **涉及价格必须注明复权格式**（老板硬性要求）：
   - ✅「当前价 1307.88（前复权）」
   - ✅「2015-06 高点 1399.71（后复权）」
   - ❌ 只报价格不报口径
2. **判断逻辑来自 holdle_ask 检索的方法论片段**，不凭空编造
3. **红线**：不荐股、不预测、不承诺收益；结论型问题引导用户自己判断
4. **风险声明**：回答末尾附「以上为 HOLDLE 方法论知识讲解，不构成投资建议」

---

## 五、数据源说明

| 数据 | 来源 | 说明 |
|:--|:--|:--|
| 月K/周K/日K | TickFlow（免费）→ Baostock 降级 | 后复权/前复权均支持 |
| 财报 | AkShare（东方财富） | **A股/港股/美股** 近20年 |
| 实时行情 | 腾讯（A/港）/ 新浪（美股） | |

依赖：先检查 `python3 -c "import baostock, tickflow, akshare, pandas"`，缺哪个装哪个（`pip3 install <缺的包>`）

---

## 六、版本更新（重要）

**脚本自带自更新机制**（2026-08-19 起）：

1. **每次运行自动检查**（每天最多一次）：脚本启动时会对比 GitHub 远程版本
2. **有新版本** → 脚本自动下载新版覆盖自己 → 提示重新运行
3. 如果自动更新失败，手动更新：
   ```bash
   cd <holdle-data-skill目录>
   git pull            # 如果当初是 git clone 的
   # 或
   python3 holdle_data.py --update
   ```

**作为 agent 的职责：**
- 如果脚本输出「检测到新版本」提示，**帮用户执行更新**（`python3 holdle_data.py --update` 或 git pull）
- 更新后重新运行脚本，确认数据正常
- 不要忽视版本提示（旧版可能因数据源接口变化而报错）

> 查看当前版本：`python3 holdle_data.py --version`

---

## 七、常见问题

- **数据拉取失败？** 先检查是否版本过旧（运行 `--update`），再查网络 + 依赖（先检查再装，见上文）
- **代码识别不了？** A股用6位数字，美股用字母代码（NVDA），港股加 hk_ 前缀（hk_00700）
- **历史数据缺失？** 后复权模式（默认）数据最全；前复权可能因分红送股导致早期数据被压缩
